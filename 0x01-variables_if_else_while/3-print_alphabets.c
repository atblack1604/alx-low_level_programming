#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Successful only when 0
 */

int main(void)
{
	char c;
	
	char w;

	c = 'a';
	w = 'A';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	while
		(w <= 'z') {
			putchar (w);
			w++;
		}
	putchar('\n');
	return (0);
}
